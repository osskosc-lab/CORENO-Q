# Correlon-LX v0.2

Phase 0 freezes a common cross-theory packet and repairs the v0.1 nonlinear blind spot with a fixed random-Fourier-feature normalized HSIC proxy.

Scientific role: candidate-edge generator only.

The six-theory real-data scan is intentionally not executed until each theory has a compatible residual packet with provenance. Static theorem objects and hypothesis-only quantities are never silently converted into pseudo-time-series.

See:
- correlon_lx_v0.2_interface_freeze.json
- correlon_lx_v0.2_phase0_pilot_results.json
