# Correlon-LX v0.2.1 execution workspace

This workspace is a fresh revision. It does not modify or resume v0.2 in place.

- `src/external_manifest_freeze.py` audits Phase 0F'.
- `src/representation_precheck.py` checks whether Phase 0R is reproducibly specified before generating data.
- `tests/` contains gate tests.
- `artifacts/cycle0f-prime/` records the fresh-clone Repository Freeze.
- `artifacts/cycle1-run1/` is the only permitted fresh Cycle 1 namespace.

No synthetic data are generated until the representation implementation is fully preregistered.

