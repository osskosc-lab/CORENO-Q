# Correlon-LX v0.2.2

This is a fresh Executable Representation Freeze. It closes the Phase 0R implementation gaps recorded by v0.2.1 without changing any earlier STOP record.

The historical v0.2 threshold is retained only as provenance. v0.2.2 independently calibrates each representation with 1000 null replicates and evaluates 200 fresh datasets per condition.

No parameter may be changed after Phase 0R results are observed. Failure ends this revision with `STOP_REPRESENTATION_INSTABILITY`; any modification requires a fresh v0.2.3 revision.

